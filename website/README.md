# Grammar Llama website

Next.js landing page for the Grammar Llama Mac app.

```bash
npm install
npm run dev
```

Deploy on Vercel with **Root Directory** set to `website`. Update `app/config.ts` with the release download URL.
